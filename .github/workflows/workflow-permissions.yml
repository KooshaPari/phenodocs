name: workflow-permissions
on: [push, pull_request]
permissions:
  contents: read
  pull-requests: write
  statuses: write
  security-events: write

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - name: OK
        run: echo "workflow permissions baseline"
