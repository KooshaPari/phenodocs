name: Deploy docs
on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - '.github/workflows/docs-deploy.yml'
      - '.github/workflows/vitepress-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    uses: KooshaPari/phenoShared/.github/workflows/vitepress-pages.yml@4b820a6d1eca7884e692e4ec4edf2b59732809cd
    with:
      concurrency_group: pages-phenoshared
      docs_dir: docs
