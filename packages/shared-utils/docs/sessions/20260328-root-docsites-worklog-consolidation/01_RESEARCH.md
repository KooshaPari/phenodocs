# Research

- Repo-local worklog state is tracked through AgilePlus.
- The root worklog points at AgilePlus for active tracking and Git history for merged work.
- A canonical session bundle is needed so later sweeps can resume from `docs/sessions` rather than only the root worklog file.
