# Known Issues

## Deferred Merge

`phenodocs-scorecard-remediation` is a detached checkout with unrelated `ADRS.md`, `CHANGELOG.md`, `SOTA.md`, and `SPEC.md` changes. This badge work is prepared in an isolated worktree and should be merged only after those changes are reconciled or preserved intentionally.

