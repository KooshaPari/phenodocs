# Testing Strategy

## Validation

- Search README and session docs for the Sladge badge URL and label.
- Review `git diff --stat` to confirm the change is docs-only.
- Confirm the isolated worktree is clean after commit.

## Runtime Tests

No runtime tests are required because this is a README and documentation-only governance badge change.

