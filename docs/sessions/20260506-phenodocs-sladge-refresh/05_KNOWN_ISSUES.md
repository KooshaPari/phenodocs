# Known Issues

- Canonical `phenodocs` has unrelated local workflow and `.agileplus` changes.
- `phenodocs-scorecard-remediation` has unrelated workflow, ADR, changelog,
  SOTA, SPEC, README, and `FUNDING.yml` changes.
- The older `docs-phenodocs-sladge-badge` branch is clean but not an ancestor
  of the current active head.

These are integration blockers only; they are not caused by the README badge
refresh.
