# Implementation Strategy

The work is intentionally README/spec/session-doc scoped. The refreshed branch
starts from current `ci/add-mypy` head so it supersedes the older
`docs-phenodocs-sladge-badge` branch without requiring a merge through dirty
worktrees.

No generated docs, VitePress temp output, workflow files, or scorecard
remediation files are edited in this change.
