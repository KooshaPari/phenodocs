# Research

The current README describes PhenoDocs as a federation hub that indexes content
for human and AI consumption and generates `.llms.txt` for LLM context. That is
direct LLM-context documentation infrastructure, so the project remains in scope
for Sladge disclosure.

Existing prepared evidence under `worktrees/phenodocs/docs/sladge-current` is
based on `ci/add-mypy`, is not an ancestor of the active
`fix/tsconfig-strict-mode` branch, and would bring broad unrelated workflow and
documentation changes. This refresh starts from the live branch head instead.
